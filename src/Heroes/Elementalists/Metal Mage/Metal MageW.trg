{
  "Id": 50333266,
  "Comment": "",
  "IsScript": true,
  "RunOnMapInit": false,
  "Script": "scope MetalMageW initializer init\r\n\r\n\tfunction Trig_Metal_MageW_Conditions takes nothing returns boolean\r\n\t    return GetSpellAbilityId() == 'A0KS'\r\n\tendfunction\r\n\t\r\n\tfunction Metal_MageWCast takes nothing returns nothing\r\n\t    local integer id = GetHandleId( GetExpiredTimer( ) )\r\n\t    local real dmg = LoadReal( udg_hash, id, StringHash( \"mtmw\" ) )\r\n\t    local unit target = LoadUnitHandle( udg_hash, id, StringHash( \"mtmw\" ) )\r\n\t    local unit caster = LoadUnitHandle( udg_hash, id, StringHash( \"mtmwc\" ) )\r\n\t    local boolean isAlive = IsUnitAlive(target)\r\n\t    \r\n\t    if isAlive == false or GetUnitAbilityLevel( target, 'B0AZ') == 0 then\r\n\t        call DestroyTimer( GetExpiredTimer() )\r\n\t        call FlushChildHashtable( udg_hash, id )\r\n\t    elseif isAlive then\r\n\t        call UnitDamageTarget( caster, target, dmg, true, false, ATTACK_TYPE_NORMAL, DAMAGE_TYPE_MAGIC, WEAPON_TYPE_WHOKNOWS )\r\n\t    endif\r\n\t    \r\n\t    set caster = null\r\n\t    set target = null\r\n\tendfunction\r\n\t\r\n\tfunction Trig_Metal_MageW_Actions takes nothing returns nothing\r\n\t    local unit dummy\r\n\t    local integer id\r\n\t    local real dmg\r\n\t    local unit caster\r\n\t    local unit target\r\n\t    local integer lvl\r\n\t    local real t\r\n\t    \r\n\t    if CastLogic() then\r\n\t        set caster = udg_Caster\r\n\t        set target = udg_Target\r\n\t        set lvl = udg_Level\r\n\t        set t = udg_Time\r\n\t    elseif RandomLogic() then\r\n\t        set caster = udg_Caster\r\n\t        set target = randomtarget( caster, 300, \"enemy\", 0, 0, 0 )\r\n\t        set lvl = udg_Level\r\n\t        call textst( udg_string[0] + GetObjectName('A0KS'), caster, 64, 90, 10, 1.5 )\r\n\t        set t = lvl + 2\r\n\t        if target == null then\r\n\t            set caster = null\r\n\t            return\r\n\t        endif\r\n\t    else\r\n\t        set caster = GetSpellAbilityUnit()\r\n\t        set target = GetSpellTargetUnit()\r\n\t        set lvl = GetUnitAbilityLevel(GetSpellAbilityUnit(), GetSpellAbilityId())\r\n\t        set t = lvl + 2\r\n\t    endif\r\n\t    set t = timebonus(caster, t)\r\n\t    \r\n\t    set dmg = 50\r\n\t\r\n\t    call DestroyEffect( AddSpecialEffect( \"Dark Execution.mdx\", GetUnitX(target), GetUnitY(target) ) )\r\n\t\r\n\t\tcall bufallst( caster, target, 'A1HY', 0, 0, 0, 0, 'B0AZ', \"metal_mage_w_buff\", t )\r\n\t    \r\n\t    set id = InvokeTimerWithUnit( target, \"mtmw\", 1, true, function Metal_MageWCast )\r\n\t    call SaveUnitHandle( udg_hash, id, StringHash( \"mtmwc\" ), caster )\r\n\t    call SaveReal( udg_hash, id, StringHash( \"mtmw\" ), dmg)\r\n\t    \r\n\t    set caster = null\r\n\t    set target = null\r\n\tendfunction\r\n\t\r\n\t//===========================================================================\r\n\tprivate function init takes nothing returns nothing\r\n\t    set gg_trg_Metal_MageW = CreateTrigger(  )\r\n\t    call TriggerRegisterAnyUnitEventBJ( gg_trg_Metal_MageW, EVENT_PLAYER_UNIT_SPELL_EFFECT )\r\n\t    call TriggerAddCondition( gg_trg_Metal_MageW, Condition( function Trig_Metal_MageW_Conditions ) )\r\n\t    call TriggerAddAction( gg_trg_Metal_MageW, function Trig_Metal_MageW_Actions )\r\n\tendfunction\r\n\r\nendscope",
  "Events": [],
  "LocalVariables": [],
  "Conditions": [],
  "Actions": []
}