{
  "Id": 50333357,
  "Comment": "",
  "IsScript": true,
  "RunOnMapInit": false,
  "Script": "scope ShoggothW initializer init\r\n\r\n\tglobals\r\n\t\tprivate constant integer ABILITY_ID = 'A195'\r\n\t\t\r\n\t\tprivate constant integer AREA_RANGE = 350\r\n\t\tprivate constant integer TENTACLE_SPAWN_RANGE = 200\r\n\tendglobals\r\n\r\n\tprivate function condition takes nothing returns boolean\r\n\t    return GetSpellAbilityId() == ABILITY_ID\r\n\tendfunction\r\n\t\r\n\tprivate function Teleportation takes unit caster, integer level, real x, real y, real duration returns nothing\r\n\t\tlocal group g = CreateGroup()\r\n\t    local unit u\r\n\t    local location teleportationPoint = Location(x, y)\r\n\t    local boolean isTeleport = RectContainsLoc(udg_Boss_Rect, teleportationPoint )\r\n\t\t\r\n\t    call GroupEnumUnitsInRange( g, x, y, AREA_RANGE, null )\r\n\t    loop\r\n\t        set u = FirstOfGroup(g)\r\n\t        exitwhen u == null\r\n\t        if unitst( u, caster, \"enemy\" ) then\r\n\t            if GetUnitDefaultMoveSpeed(u) != 0 then\r\n\t                call DestroyEffect( AddSpecialEffect(\"Void Teleport Caster.mdx\", GetUnitX(u), GetUnitY(u) ) )\r\n\t                if isTeleport then\r\n\t                \tcall SetUnitPositionLoc( u, teleportationPoint )\r\n                \tendif\r\n\t                call DestroyEffect( AddSpecialEffect( \"Void Teleport Caster.mdx\", GetUnitX(u), GetUnitY(u) ) )\r\n\t            endif\r\n\t            call bufallst( caster, u, 'A196', 'A197', 0, 0, 0, 'B02B', \"shgw\", duration )\r\n\t            call SetUnitAbilityLevel( u, 'A197', level )\r\n\t        endif\r\n\t        call GroupRemoveUnit(g,u)\r\n\t    endloop\r\n\t    \r\n\t    call RemoveLocation(teleportationPoint)\r\n\t    call DestroyGroup( g )\r\n\t    set teleportationPoint = null\r\n\t    set u = null\r\n\t    set g = null\r\n\tendfunction\r\n\t\r\n\tprivate function TentacleSpawn takes unit caster, integer level, real x, real y returns nothing\r\n\t\tlocal real spawnX\r\n\t    local real spawnY\r\n\t    local integer i\r\n\t    local integer amountToSpawn\r\n\t    local real r\r\n\t\r\n\t\tset amountToSpawn = 3 + level\r\n\t    set r = 360 / amountToSpawn\r\n\t    set i = 1\r\n\t    loop\r\n\t        exitwhen i > amountToSpawn\r\n\t        set spawnX = x + TENTACLE_SPAWN_RANGE * Cos( r * i * bj_DEGTORAD)\r\n\t        set spawnY = y + TENTACLE_SPAWN_RANGE * Sin( r * i * bj_DEGTORAD)\r\n\t        set bj_lastCreatedUnit = CreateUnit( GetOwningPlayer(caster), 'n03F', spawnX, spawnY, 270 )\r\n\t        call UnitApplyTimedLife( bj_lastCreatedUnit , 'BTLF', 15 )\r\n\t        call SetUnitAnimation( bj_lastCreatedUnit, \"birth\" )\r\n\t        call QueueUnitAnimation( bj_lastCreatedUnit, \"stand\" )\r\n\t        set i = i + 1\r\n\t    endloop\r\n\tendfunction\r\n\t\r\n\tprivate function action takes nothing returns nothing\r\n\t    local unit caster\r\n\t    local integer lvl\r\n\t    local real x\r\n\t    local real y\r\n\t    local real t\r\n\r\n\t    if CastLogic() then\r\n\t        set caster = udg_Caster\r\n\t        set x = GetLocationX( GetSpellTargetLoc() )\r\n\t        set y = GetLocationY( GetSpellTargetLoc() )\r\n\t        set lvl = udg_Level\r\n\t        set t = udg_Time\r\n\t    elseif RandomLogic() then\r\n\t        set caster = udg_Caster\r\n\t        set lvl = udg_Level\r\n\t        set x = GetUnitX( caster ) + GetRandomReal( -650, 650 )\r\n\t        set y = GetUnitY( caster ) + GetRandomReal( -650, 650 )\r\n\t        call textst( udg_string[0] + GetObjectName(ABILITY_ID), caster, 64, 90, 10, 1.5 )\r\n\t        set t = 10\r\n\t    else\r\n\t        set caster = GetSpellAbilityUnit()\r\n\t        set lvl = GetUnitAbilityLevel(GetSpellAbilityUnit(), GetSpellAbilityId())\r\n\t        set x = GetLocationX( GetSpellTargetLoc() )\r\n\t        set y = GetLocationY( GetSpellTargetLoc() )\r\n\t        set t = 10\r\n\t    endif\r\n\t    set t = timebonus(caster, t)\r\n\t    \r\n\t    call Teleportation(caster, lvl, x, y, t)\r\n\t    call TentacleSpawn(caster, lvl, x, y)\r\n\r\n\t    set caster = null\r\n\tendfunction\r\n\t\r\n\t//===========================================================================\r\n\tprivate function init takes nothing returns nothing\r\n\t    set gg_trg_ShoggothW = CreateTrigger(  )\r\n\t    call TriggerRegisterAnyUnitEventBJ( gg_trg_ShoggothW, EVENT_PLAYER_UNIT_SPELL_EFFECT )\r\n\t    call TriggerAddCondition( gg_trg_ShoggothW, Condition( function condition ) )\r\n\t    call TriggerAddAction( gg_trg_ShoggothW, function action )\r\n\tendfunction\r\n\t\r\nendscope",
  "Events": [],
  "LocalVariables": [],
  "Conditions": [],
  "Actions": []
}